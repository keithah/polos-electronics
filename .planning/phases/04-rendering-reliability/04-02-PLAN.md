---
phase: 04-rendering-reliability
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - layouts/index.html
  - layouts/partials/reviews-reliability-meta.html
autonomous: true

must_haves:
  truths:
    - "Reviews section always renders review cards from live or fallback data"
    - "Review freshness metadata is visible with low-emphasis styling intent"
    - "Fallback reviews preserve per-review platform attribution"
    - "No warning-heavy outage messaging appears in review section"
  artifacts:
    - path: "layouts/index.html"
      provides: "Deterministic review rendering logic"
      contains: "fallbackReviews"
    - path: "layouts/partials/reviews-reliability-meta.html"
      provides: "Reusable freshness metadata block"
      contains: "lastUpdated"
  key_links:
    - from: "layouts/index.html"
      to: "data/reviews.json"
      via: "Site.Data.reviews"
      pattern: "reviews|fallbackReviews|staleAfterDays"
---

<objective>
Implement deterministic Reviews rendering and visible freshness metadata.

Purpose: Satisfy REND-01 and REND-03 by ensuring users always see authentic review content plus update recency context.

Output: Updated review rendering in `layouts/index.html` and reusable reliability metadata partial.
</objective>

<execution_context>
@/Users/keith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rendering-reliability/04-CONTEXT.md
@.planning/phases/04-rendering-reliability/04-RESEARCH.md
@.planning/phases/04-rendering-reliability/04-01-SUMMARY.md
@layouts/index.html
@data/reviews.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace review empty-state branch with deterministic card fallback</name>
  <files>layouts/index.html</files>
  <action>
Refactor review display selection logic to compute `$displayReviews` deterministically:

1. Build from `.Site.Data.reviews.reviews` (existing 4+ stars + nextdoor rule).
2. If empty, build from `.Site.Data.reviews.fallbackReviews` using same card markup structure.
3. Keep carousel wrapper and platform CTA block intact.

Requirements:
- Section heading and card layout remain consistent across live/fallback states.
- Keep platform badge and source link per card.
- Remove old "reviews are being refreshed" paragraph to avoid non-deterministic empty section state.
  </action>
  <verify>
- `grep -n "fallbackReviews" layouts/index.html` returns at least 1 match
- No review-empty-state paragraph remains
- `hugo --gc --minify` completes without template errors
  </verify>
  <done>
- Reviews section no longer collapses when live review array is empty
- Fallback cards render in same visual shell as live cards
  </done>
</task>

<task type="auto">
  <name>Task 2: Add freshness metadata partial and integrate it under reviews title</name>
  <files>layouts/partials/reviews-reliability-meta.html, layouts/index.html</files>
  <action>
Create `layouts/partials/reviews-reliability-meta.html` that:

- Reads `.Site.Data.reviews.lastUpdated`
- Parses age in days with `time.AsTime` and `time.Now`
- Compares against `.Site.Data.reviews.staleAfterDays` (default 45)
- Renders low-emphasis text such as:
  - `Updated <date>`
  - `Refresh in progress` (only when stale threshold exceeded)

Integrate the partial directly beneath the reviews section title.

Tone constraints from context:
- No outage-style language
- Keep concise and neutral
  </action>
  <verify>
- `ls layouts/partials/reviews-reliability-meta.html` confirms partial exists
- `grep -n "reviews-reliability-meta" layouts/index.html` shows partial call
- Freshness text appears in rendered `public/index.html` after build
  </verify>
  <done>
- Users can always see when review data was last updated
- Staleness is communicated without trust-eroding copy
  </done>
</task>

</tasks>

<verification>
```bash
hugo --gc --minify
rg "Updated|Refresh in progress" public/index.html
rg "fallbackReviews" layouts/index.html
```
</verification>

<success_criteria>
- Reviews section always shows cards from live or fallback source
- Per-review attribution remains visible in fallback mode
- Freshness metadata is visible and stale state is distinguishable
- Build succeeds with no template/runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-rendering-reliability/04-02-SUMMARY.md`
</output>
