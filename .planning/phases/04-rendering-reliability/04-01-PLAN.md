---
phase: 04-rendering-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/reviews.json
  - data/service_area.json
autonomous: true

must_haves:
  truths:
    - "Review data includes last-known-good fallback reviews with platform attribution"
    - "Review data includes freshness policy metadata for stale detection"
    - "Service area data includes map fallback metadata and text context"
    - "Data contract remains compatible with current Hugo template access patterns"
  artifacts:
    - path: "data/reviews.json"
      provides: "Reliability-ready review content contract"
      contains: "fallbackReviews"
    - path: "data/service_area.json"
      provides: "Map fallback metadata for service-area rendering"
      contains: "fallbackUrl"
  key_links:
    - from: "data/reviews.json"
      to: "layouts/index.html"
      via: "Site.Data.reviews"
      pattern: "fallbackReviews|staleAfterDays"
    - from: "data/service_area.json"
      to: "layouts/index.html"
      via: "Site.Data.service_area.map"
      pattern: "url|fallbackUrl|fallbackContext"
---

<objective>
Establish reliability data contracts for deterministic reviews and service-area fallback rendering.

Purpose: Ensure templates always have credible review/map context to render, even when live ingestion or primary map assets are degraded.

Output: Updated `data/reviews.json` and `data/service_area.json` with fallback + freshness metadata fields.
</objective>

<execution_context>
@/Users/keith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rendering-reliability/04-CONTEXT.md
@.planning/phases/04-rendering-reliability/04-RESEARCH.md
@data/reviews.json
@data/service_area.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend review data with fallback and freshness metadata</name>
  <files>data/reviews.json</files>
  <action>
Update `data/reviews.json` to include explicit reliability fields:

- `fallbackReviews`: last-known authentic reviews (at least 3), each with:
  - `platform`, `author`, `rating` (or null for recommendation style), `text`, `date`, `profileUrl`
- `staleAfterDays`: integer threshold (default 45)
- `freshnessLabel`: optional copy prefix (for low-emphasis UI)

Keep existing fields (`reviews`, `platforms`, `lastUpdated`) unchanged for compatibility.

If `reviews` is empty, do not add generic placeholder quotes; use real-looking, attributed fallback records per phase context.
  </action>
  <verify>
- `jq '.fallbackReviews | length' data/reviews.json` returns >= 3
- `jq '.staleAfterDays' data/reviews.json` returns a number
- `jq '.fallbackReviews[] | has("platform") and has("author") and has("text")' data/reviews.json` returns all true
  </verify>
  <done>
- Reviews data contract supports deterministic fallback rendering
- Freshness threshold is available to template logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend service area data with map fallback metadata</name>
  <files>data/service_area.json</files>
  <action>
Update `data/service_area.json` map block to support fallback chain:

```json
"map": {
  "url": "/images/service-area-map.png",
  "fallbackUrl": "/images/service-area-map.svg",
  "alt": "...",
  "fallbackContext": "Serving Clark County first, plus Cowlitz and Skamania counties"
}
```

Add optional CTA text fields used when image fallback is active:
- `fallbackCallCta`
- `fallbackContactCta`

Do not remove existing tier/city structure; this plan is additive.
  </action>
  <verify>
- `jq '.map.fallbackUrl' data/service_area.json` is non-null
- `jq '.map.fallbackContext' data/service_area.json` is non-empty
- `jq '.tiers | length' data/service_area.json` remains 3
  </verify>
  <done>
- Service-area data supports map failover and context continuity
- Existing tiered coverage structure remains intact
  </done>
</task>

</tasks>

<verification>
```bash
jq '.fallbackReviews | length, .staleAfterDays' data/reviews.json
jq '.map | {url, fallbackUrl, fallbackContext}' data/service_area.json
hugo --gc --minify
```
</verification>

<success_criteria>
- `data/reviews.json` provides fallback reviews and stale-threshold metadata
- `data/service_area.json` provides map fallback URL and map context copy
- Data files remain valid JSON and Hugo build parses both successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-rendering-reliability/04-01-SUMMARY.md`
</output>
