---
phase: 04-rendering-reliability
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - layouts/index.html
  - assets/css/custom.css
autonomous: false

must_haves:
  truths:
    - "Service Area section keeps list/map context visible even if primary map image fails"
    - "Primary map failure falls back to secondary asset or textual map context without alarm messaging"
    - "Fallback context includes call and contact CTAs"
    - "Service-area list remains first-class content regardless of map state"
  artifacts:
    - path: "layouts/index.html"
      provides: "Map failover rendering and fallback context block"
      contains: "fallbackContext"
    - path: "assets/css/custom.css"
      provides: "Styling for map fallback states and low-emphasis metadata"
      contains: "service-area-map-fallback"
  key_links:
    - from: "layouts/index.html"
      to: "data/service_area.json"
      via: "Site.Data.service_area.map"
      pattern: "url|fallbackUrl|fallbackContext|fallbackCallCta|fallbackContactCta"
---

<objective>
Implement seamless map fallback behavior for Service Area reliability.

Purpose: Satisfy REND-02 while preserving a continuity-first user experience that keeps service-area context and CTAs visible.

Output: Updated service-area template and CSS handling for map image failure states.
</objective>

<execution_context>
@/Users/keith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rendering-reliability/04-CONTEXT.md
@.planning/phases/04-rendering-reliability/04-RESEARCH.md
@.planning/phases/04-rendering-reliability/04-01-SUMMARY.md
@layouts/index.html
@assets/css/custom.css
@data/service_area.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add map failover chain and fallback context block</name>
  <files>layouts/index.html</files>
  <action>
Update the service-area map panel to support graceful failover:

- Render primary `<img>` from `map.url`.
- Add `onerror` logic to swap once to `map.fallbackUrl` when present.
- If fallback image also fails (or missing), show an inline `.service-area-map-fallback` block with:
  - `map.fallbackContext`
  - call CTA link (`tel:`)
  - contact CTA anchor (`#contact`)

Implementation constraints:
- Keep service-area text/list content as the primary context block (no dependency on map success).
- Do not show outage/unavailable alerts.
- Ensure fallback block exists in DOM and toggles visible only when image cannot render.
  </action>
  <verify>
- `grep -n "fallbackUrl" layouts/index.html` returns at least 1
- `grep -n "service-area-map-fallback" layouts/index.html` returns at least 1
- Manual image-path break test shows fallback block appears with both CTAs
  </verify>
  <done>
- Map panel no longer fails blank when primary image is unavailable
- Fallback context and CTAs remain available without disruptive messaging
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for map fallback presentation and metadata polish</name>
  <files>assets/css/custom.css</files>
  <action>
Add styles for:

- `.reviews-reliability-meta` and stale modifier class (muted, low-emphasis)
- `.service-area-map-fallback` card/container
- CTA row inside fallback block to match site button language

Styling goals:
- Seamless visual continuity with existing card system
- No warning palette or error-banner look
- Mobile-safe layout for fallback CTAs
  </action>
  <verify>
- `grep -n "reviews-reliability-meta" assets/css/custom.css` returns at least 1
- `grep -n "service-area-map-fallback" assets/css/custom.css` returns at least 1
- Mobile viewport keeps fallback CTAs readable and tappable
  </verify>
  <done>
- Fallback states look intentional and consistent with current design
- Freshness metadata remains visible but non-intrusive
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Deterministic reviews + map failover rendering with seamless fallback context and CTAs</what-built>
  <how-to-verify>
1. Run `hugo server` and open homepage.
2. Reviews section:
   - Confirm review cards render even when `data/reviews.json` live `reviews` is empty.
   - Confirm "Updated ..." metadata appears below heading.
3. Service area section:
   - Temporarily break `map.url` in `data/service_area.json` and refresh.
   - Confirm map fallback URL is attempted, then fallback context block appears if image fails.
   - Confirm both CTAs are visible: call link and contact anchor.
4. Confirm no alarming outage copy appears in either section.
5. Check mobile width (~375px): fallback block and CTAs remain readable.
  </how-to-verify>
  <resume-signal>Type "approved" if fallback behavior is seamless and reliable, or describe adjustments needed</resume-signal>
</task>

</tasks>

<verification>
```bash
hugo --gc --minify
rg "service-area-map-fallback|reviews-reliability-meta" layouts/index.html assets/css/custom.css
```
</verification>

<success_criteria>
- Service Area context remains visible even when map image assets fail
- Map panel failover chain works without blank state
- Reviews and Service Area reliability metadata/fallback UX is seamless and non-alarmist
- Human verification confirms desktop and mobile behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-rendering-reliability/04-03-SUMMARY.md`
</output>
