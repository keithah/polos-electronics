---
phase: 09-crawling-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - layouts/robots.txt
  - layouts/index.html
autonomous: true

must_haves:
  truths:
    - "Search engines find sitemap.xml via robots.txt"
    - "All images have explicit dimensions preventing layout shift"
    - "Hero image loads before other images in network waterfall"
    - "Map embed reserves space before iframe loads"
  artifacts:
    - path: "layouts/robots.txt"
      provides: "Hugo template generating robots.txt with sitemap directive"
      contains: "Sitemap:"
    - path: "layouts/index.html"
      provides: "Image elements with width/height attributes and hero fetchpriority"
      contains: "fetchpriority"
  key_links:
    - from: "layouts/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap directive with absURL"
      pattern: 'Sitemap:.*sitemap\.xml'
    - from: "layouts/index.html (hero)"
      to: "images/hero-image.jpg"
      via: "fetchpriority=high attribute"
      pattern: 'fetchpriority="high"'
---

<objective>
Fix crawling issues and image loading for SEO and performance

Purpose: Search engines need a valid robots.txt with sitemap reference, and images need explicit dimensions to prevent Cumulative Layout Shift (CLS). The hero image needs fetchpriority="high" for faster LCP.

Output: Hugo robots.txt template, updated image elements with dimensions and priority hints
</objective>

<execution_context>
@/Users/keith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-crawling-quick-wins/09-RESEARCH.md
@layouts/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create robots.txt Hugo template with sitemap directive</name>
  <files>layouts/robots.txt</files>
  <action>
Create a Hugo template at `layouts/robots.txt` that generates a valid robots.txt file:

```
User-agent: *
Allow: /

Sitemap: {{ "sitemap.xml" | absURL }}
```

Notes:
- Cloudflare will PREPEND their AI crawl control directives before this content (this is expected behavior)
- Hugo's sitemap is already generated at the root level
- Use `absURL` filter to generate full URL (https://poloselectronics.com/sitemap.xml)
- Do NOT include any Cloudflare-specific directives (they manage those)
  </action>
  <verify>
1. Run `hugo` to build
2. Check `public/robots.txt` contains "Sitemap:" line with full URL
3. Verify format: `Sitemap: https://poloselectronics.com/sitemap.xml`
  </verify>
  <done>robots.txt template exists and build output contains sitemap directive with absolute URL</done>
</task>

<task type="auto">
  <name>Task 2: Add image dimensions and hero fetchpriority</name>
  <files>layouts/index.html</files>
  <action>
Update all `<img>` elements in layouts/index.html with explicit width and height attributes using INTRINSIC (file) pixel dimensions.

**Image dimensions (verified from research):**

| Image | Width | Height | Notes |
|-------|-------|--------|-------|
| hero-image.jpg | 1500 | 1000 | ADD fetchpriority="high", NO loading="lazy" |
| logo.png | 1500 | 439 | Header logo |
| andy-polos.jpg | 960 | 958 | Team section (currently 300x300 - change to intrinsic) |
| john-polos.jpg | 500 | 500 | Team section (currently 300x300 - change to intrinsic) |
| derick-steele.jpg | 907 | 680 | Team section (currently 300x300 - change to intrinsic) |
| services-image.jpg | 600 | 900 | Services section (currently 500x400 - change to intrinsic) |
| contact-image.jpg | 500 | 500 | Contact section (currently 500x400 - change to intrinsic) |
| service-area-preview.jpg | 762 | 620 | Map preview |

**Specific changes:**

1. **Hero image (line ~131):**
   - Add `width="1500" height="1000"`
   - Add `fetchpriority="high"`
   - Ensure NO loading="lazy" (it should not have one currently)

2. **Header logo (line ~95):**
   - Add `width="1500" height="439"`

3. **Team images (andy-polos, derick-steele, john-polos):**
   - Change from `width="300" height="300"` to intrinsic dimensions
   - Keep `loading="lazy"`

4. **Services image (line ~231):**
   - Change from `width="500" height="400"` to `width="600" height="900"`
   - Keep `loading="lazy"`

5. **Contact image (line ~487):**
   - Change from `width="500" height="400"` to `width="500" height="500"`
   - Keep `loading="lazy"`

6. **Service area preview image (line ~306):**
   - Add `width="762" height="620"`
   - Add `loading="lazy"` (below fold)

7. **Footer logo (line ~543):**
   - Change from `width="150" height="auto"` to `width="1500" height="439"`
   - Keep `loading="lazy"`

**Anti-patterns to avoid:**
- Do NOT add fetchpriority to any image except the hero
- Do NOT add loading="lazy" to the hero image
- Do NOT use CSS display dimensions - use intrinsic file dimensions
  </action>
  <verify>
1. Run `hugo server` and view page source
2. Verify hero image has `fetchpriority="high"` and NO `loading="lazy"`
3. Verify all images have width and height attributes with correct values
4. Check Network tab: hero-image.jpg should appear early in waterfall
5. Run Lighthouse or PageSpeed to verify CLS improvement
  </verify>
  <done>All images have intrinsic dimensions, hero has fetchpriority="high", no loading="lazy" on hero</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. **robots.txt validation:**
   ```bash
   hugo && grep "Sitemap:" public/robots.txt
   ```
   Expected: `Sitemap: https://poloselectronics.com/sitemap.xml`

2. **Image dimensions check:**
   ```bash
   grep -E 'width="[0-9]+" height="[0-9]+"' layouts/index.html | wc -l
   ```
   Expected: At least 8 images with dimensions

3. **Hero fetchpriority check:**
   ```bash
   grep -A2 'hero-image.jpg' layouts/index.html | grep 'fetchpriority="high"'
   ```
   Expected: fetchpriority="high" present

4. **No lazy on hero check:**
   ```bash
   grep -A3 'hero-image.jpg' layouts/index.html | grep 'loading="lazy"'
   ```
   Expected: No match (hero should NOT have lazy loading)

5. **Local visual verification:**
   - Run `hugo server`
   - Verify page loads without layout shift
   - Verify hero image appears early
</verification>

<success_criteria>
1. robots.txt template exists at layouts/robots.txt
2. Built robots.txt contains sitemap directive with absolute URL
3. All img elements have explicit width and height attributes
4. Hero image has fetchpriority="high" and no loading="lazy"
5. Hugo build completes without errors
6. CLS-02 already satisfied (map container has fixed 400px/350px height)
</success_criteria>

<output>
After completion, create `.planning/phases/09-crawling-quick-wins/09-01-SUMMARY.md`
</output>
