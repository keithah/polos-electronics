---
phase: 08-seo-enhancement
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - layouts/index.html
autonomous: true

must_haves:
  truths:
    - "All below-the-fold images have lazy loading enabled"
    - "Images have explicit width and height for layout stability"
  artifacts:
    - path: "layouts/index.html"
      provides: "Images with loading='lazy' and dimensions"
      contains: 'loading="lazy"'
  key_links:
    - from: "layouts/index.html"
      to: "static/images/*"
      via: "img tags with lazy loading"
      pattern: 'img.*loading="lazy"'
---

<objective>
Add native lazy loading to below-the-fold images for improved page speed and Core Web Vitals.

Purpose: SEO-06 requires image optimization. Native HTML lazy loading (`loading="lazy"`) has 95%+ browser support and requires no JavaScript.

Output: Updated index.html with lazy loading on appropriate images (not hero image - needs immediate load for LCP).
</objective>

<execution_context>
@/Users/keith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-seo-enhancement/08-RESEARCH.md
@layouts/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lazy loading to below-the-fold images</name>
  <files>layouts/index.html</files>
  <action>
Add `loading="lazy"` attribute to all img tags EXCEPT the hero image (which should load immediately for good LCP score).

Images to add lazy loading to:
1. Team member images (andy-polos.jpg, derick-steele.jpg, john-polos.jpg) - around lines 143, 153, 163
2. Services image (services-image.jpg) - around line 217
3. Contact image (contact-image.jpg) - around line 466
4. Footer logo - around line 517

DO NOT add lazy loading to:
- Hero image (line 129) - this is above the fold, critical for LCP
- Logo in header (line 93) - navigation, needs immediate load

For each lazy-loaded image, also add explicit width/height if not present (prevents layout shift):
- Team photos: width="300" height="300" (circular, assume square)
- Services image: width="500" height="400" (estimate from design)
- Contact image: width="500" height="400" (estimate from design)
- Footer logo: width="150" height="auto" (maintain aspect)

Example transformation:
```html
<!-- Before -->
<img src="{{ "images/andy-polos.jpg" | relURL }}" alt="Andy Polos">

<!-- After -->
<img src="{{ "images/andy-polos.jpg" | relURL }}"
     alt="Andy Polos"
     loading="lazy"
     width="300"
     height="300">
```
  </action>
  <verify>
Run `hugo` and check:
```
# Count lazy loading attributes
grep -c 'loading="lazy"' public/index.html

# Verify hero image does NOT have lazy loading
grep -A 2 'hero-image.jpg' public/index.html | grep -v 'loading="lazy"'
```
Should show 6 lazy-loaded images (3 team + services + contact + footer logo).
Hero image should NOT have lazy loading.
  </verify>
  <done>Below-the-fold images have lazy loading, hero image loads immediately for good LCP</done>
</task>

<task type="auto">
  <name>Task 2: Verify and validate page speed improvements</name>
  <files>None (validation only)</files>
  <action>
Run Hugo development server and validate:

1. Start server: `hugo server`
2. Open browser dev tools (Network tab)
3. Verify lazy-loaded images don't load until scrolling near them
4. Verify hero image loads immediately

Document findings for summary:
- List which images are lazy loaded
- Confirm hero image is NOT lazy loaded
- Note any layout shift observations
  </action>
  <verify>
Run Hugo build and check all images:
```
hugo --gc --minify

# List all img tags with their loading attribute status
grep -n '<img' public/index.html | head -20
```

Test with Chrome Lighthouse (locally or via PageSpeed Insights after deploy):
- Performance score should not regress
- Properly Sized Images audit should pass
- Defer offscreen images audit should pass
  </verify>
  <done>Lazy loading verified working, no layout shift from added dimensions</done>
</task>

</tasks>

<verification>
1. Lazy loading present: `grep -c 'loading="lazy"' public/index.html` returns 6
2. Hero NOT lazy: `grep 'hero-image.jpg' public/index.html` shows no loading="lazy"
3. Build success: `hugo --gc --minify` completes without warnings
4. Visual check: Images load as you scroll, hero visible immediately
</verification>

<success_criteria>
- 6 images have loading="lazy" attribute
- Hero image (above fold) loads immediately without lazy attribute
- All lazy-loaded images have width/height to prevent layout shift
- Hugo build completes without errors
- Lighthouse "Defer offscreen images" audit passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-seo-enhancement/08-03-SUMMARY.md`
</output>
