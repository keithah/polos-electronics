---
phase: 08-seo-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - layouts/partials/schema/local-business.html
  - layouts/partials/schema/reviews.html
  - layouts/partials/schema/service-area-geo.html
  - layouts/index.html
autonomous: true

must_haves:
  truths:
    - "AggregateRating schema is derived from actual displayed reviews"
    - "Each review with a rating has individual Review schema markup"
    - "Service area has GeoCircle schema with Battle Ground coordinates"
  artifacts:
    - path: "layouts/partials/schema/reviews.html"
      provides: "Individual Review schema for each review"
      contains: "@type.*Review"
    - path: "layouts/partials/schema/service-area-geo.html"
      provides: "GeoCircle schema for service coverage"
      contains: "GeoCircle"
    - path: "layouts/partials/schema/local-business.html"
      provides: "AggregateRating nested in LocalBusiness"
      contains: "aggregateRating"
  key_links:
    - from: "layouts/index.html"
      to: "layouts/partials/schema/reviews.html"
      via: "partial include"
      pattern: 'partial "schema/reviews.html"'
    - from: "layouts/index.html"
      to: "layouts/partials/schema/service-area-geo.html"
      via: "partial include"
      pattern: 'partial "schema/service-area-geo.html"'
---

<objective>
Add structured data schemas for reviews (AggregateRating + individual Reviews) and service area (GeoCircle) to enhance semantic understanding by search engines.

Purpose: SEO-01, SEO-02, SEO-03 require schema enhancements for rich data understanding. While Google won't show star ratings for self-hosted LocalBusiness reviews in SERPs, the schema provides semantic value for search engines and AI systems.

Output: Three schema partials (one modified, two new) with calculated aggregate ratings, individual review markup, and geographic service coverage.
</objective>

<execution_context>
@/Users/keith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-seo-enhancement/08-RESEARCH.md
@layouts/partials/schema/local-business.html
@data/reviews.json
@data/business.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AggregateRating to LocalBusiness schema</name>
  <files>layouts/partials/schema/local-business.html</files>
  <action>
Modify the existing local-business.html partial to calculate and include AggregateRating from reviews data.

Calculate aggregate from `.Site.Data.reviews.reviews` (not fallbackReviews):
- Sum all ratings where rating exists
- Count reviews with ratings
- Calculate average (ratingValue)
- Use reviewCount for the count

Add to the $schema dict before jsonify:
```
"aggregateRating" (dict
  "@type" "AggregateRating"
  "ratingValue" (calculated average, formatted to 2 decimals)
  "reviewCount" (count of reviews with ratings)
  "bestRating" "5"
  "worstRating" "1"
)
```

Calculate the aggregate BEFORE building $schema dict, then merge it in.
Use Hugo's `add` for summation, `div` for division, `printf "%.2f"` for formatting.
  </action>
  <verify>
Run `hugo` and check output:
```
grep -A 10 "aggregateRating" public/index.html
```
Should show AggregateRating with ratingValue ~4.98 and reviewCount of 23 (reviews with ratings).
  </verify>
  <done>LocalBusiness schema includes aggregateRating calculated from actual review data</done>
</task>

<task type="auto">
  <name>Task 2: Create individual Review schema partial</name>
  <files>layouts/partials/schema/reviews.html</files>
  <action>
Create new partial `layouts/partials/schema/reviews.html` that outputs individual Review schema for each review with a rating.

Pattern from research:
```html
{{- range .Site.Data.reviews.reviews -}}
{{- if .rating -}}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Review",
  "itemReviewed": {
    "@id": "{{ $.Site.BaseURL }}#organization"
  },
  "author": {
    "@type": "Person",
    "name": "{{ .author }}"
  },
  "reviewRating": {
    "@type": "Rating",
    "ratingValue": "{{ .rating }}",
    "bestRating": "5",
    "worstRating": "1"
  },
  "reviewBody": "{{ .text }}"
}
</script>
{{- end -}}
{{- end -}}
```

Use `jsonify | safeHTML` pattern consistent with other schema partials for safety.
Reference organization via @id to link to LocalBusiness schema.
  </action>
  <verify>
Run `hugo` and check output:
```
grep -c '"@type": "Review"' public/index.html
```
Should show count of 23 (matching reviews with ratings).
  </verify>
  <done>Each review with a rating has individual Review schema markup in page head</done>
</task>

<task type="auto">
  <name>Task 3: Create GeoCircle service area schema and include new partials</name>
  <files>layouts/partials/schema/service-area-geo.html, layouts/index.html</files>
  <action>
1. Create `layouts/partials/schema/service-area-geo.html`:

```html
{{- $b := .Site.Data.business -}}
{{- $baseURL := .Site.BaseURL -}}

{{- $geoMidpoint := dict
  "@type" "GeoCoordinates"
  "latitude" $b.geo.latitude
  "longitude" $b.geo.longitude
-}}

{{- $serviceArea := dict
  "@type" "GeoCircle"
  "geoMidpoint" $geoMidpoint
  "geoRadius" "80000"
-}}

{{- $schema := dict
  "@context" "https://schema.org"
  "@type" "Service"
  "provider" (dict "@id" (printf "%s#organization" $baseURL))
  "areaServed" $serviceArea
  "name" "Low Voltage Electrical Services"
  "description" "Electrical installations, security systems, home theater, networking in Southwest Washington"
-}}

<script type="application/ld+json">
{{- $schema | jsonify | safeJS -}}
</script>
```

2. Update `layouts/index.html` head section to include new partials.
   Find the existing schema includes (around line 54-56) and add after faq.html:
```
{{ partial "schema/reviews.html" . }}
{{ partial "schema/service-area-geo.html" . }}
```
  </action>
  <verify>
Run `hugo` and check:
```
grep "GeoCircle" public/index.html
grep "geoRadius" public/index.html
```
Should find GeoCircle schema with geoRadius of 80000.
Validate with Google Rich Results Test: https://search.google.com/test/rich-results
  </verify>
  <done>Service area has GeoCircle schema with 80km radius from Battle Ground coordinates, all new partials included in page</done>
</task>

</tasks>

<verification>
1. Hugo build completes without errors: `hugo --gc`
2. LocalBusiness schema contains aggregateRating: `grep -A 5 "aggregateRating" public/index.html`
3. Review schemas present: `grep -c "@type.*Review" public/index.html` shows 23+
4. GeoCircle schema present: `grep "GeoCircle" public/index.html`
5. Test with Google Rich Results Test URL
</verification>

<success_criteria>
- AggregateRating shows ratingValue ~4.98 and reviewCount 23
- 23 individual Review schema blocks in page source
- GeoCircle schema with latitude 45.7844, longitude -122.5375, radius 80000
- All schemas validate in Google Rich Results Test (warnings acceptable, errors not)
</success_criteria>

<output>
After completion, create `.planning/phases/08-seo-enhancement/08-01-SUMMARY.md`
</output>
