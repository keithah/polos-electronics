---
phase: 03-service-area-mapping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - static/images/service-area-map.png
  - static/images/service-area-map.svg
autonomous: false

must_haves:
  truths:
    - "Map displays full Washington state outline"
    - "Clark County is visually prominent in primary red (#fe3a46)"
    - "Cowlitz and Skamania counties show in lighter tint"
    - "Battle Ground has a pin/marker icon"
  artifacts:
    - path: "static/images/service-area-map.png"
      provides: "Production-ready map image"
      min_lines: 1
    - path: "static/images/service-area-map.svg"
      provides: "Source SVG for future edits"
      contains: "clark"
  key_links:
    - from: "static/images/service-area-map.png"
      to: "data/service-area.json"
      via: "map.url reference"
      pattern: "/images/service-area-map.png"
---

<objective>
Create static service area map image with visual hierarchy.

Purpose: Visitors instantly see Clark County as primary, bordering counties as secondary, and Battle Ground headquarters location.
Output: Production PNG and source SVG in static/images/.
</objective>

<execution_context>
@/Users/keith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-service-area-mapping/03-CONTEXT.md
@.planning/phases/03-service-area-mapping/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download and prepare Washington county SVG</name>
  <files>static/images/service-area-map.svg</files>
  <action>
Download Washington state county map SVG from Wikimedia Commons or use wget/curl to fetch a blank county map.

Source options (try in order):
1. https://upload.wikimedia.org/wikipedia/commons/2/2a/Blank_map_of_Washington_state.svg
2. https://upload.wikimedia.org/wikipedia/commons/1/1a/Washington_counties_blank.svg
3. Search Wikimedia Commons for "Washington state counties blank SVG"

If downloads fail or files are unsuitable, create a simplified SVG manually with:
- Washington state outline
- Clark, Cowlitz, and Skamania county shapes (southwest corner)
- Use approximate boundaries - visual representation, not GIS accuracy

Save to static/images/service-area-map.svg

Note: The existing service-area-map.png is a 92-byte placeholder that needs replacement.
  </action>
  <verify>
- `ls -la static/images/service-area-map.svg` shows file exists and is > 1KB
- `head -5 static/images/service-area-map.svg` shows valid SVG header
  </verify>
  <done>
- SVG file exists with Washington state county outlines
- File is valid SVG format
  </done>
</task>

<task type="auto">
  <name>Task 2: Style SVG with county colors and Battle Ground pin</name>
  <files>static/images/service-area-map.svg</files>
  <action>
Edit the SVG to apply visual hierarchy:

1. **Identify county paths** - Look for path IDs or data attributes for:
   - Clark County (southwest corner, borders Columbia River)
   - Cowlitz County (west of Clark, along Columbia River)
   - Skamania County (east of Clark, Columbia River Gorge)

2. **Apply fill colors:**
   - Clark County: fill="#fe3a46" (primary red)
   - Cowlitz County: fill="#fe8a91" (lighter red tint)
   - Skamania County: fill="#fe8a91" (lighter red tint)
   - All other counties: fill="#e0e0e0" (light gray)
   - Stroke for all: stroke="#ffffff" stroke-width="1"

3. **Add Battle Ground pin marker:**
   - Battle Ground coordinates: approximately 45.78°N, 122.54°W
   - Place in Clark County, roughly center-north
   - Pin SVG path: `<path d="M12 0C5.4 0 0 5.4 0 12c0 9 12 20 12 20s12-11 12-20c0-6.6-5.4-12-12-12z" fill="#323232"/><circle cx="12" cy="10" r="4" fill="#fff"/>`
   - Wrap in `<g transform="translate(X,Y) scale(0.8)">` positioned over Battle Ground

4. **Optimize viewBox** - Ensure entire state is visible with reasonable padding

5. **Remove unnecessary elements** - Strip comments, metadata, unused defs to reduce file size
  </action>
  <verify>
- `grep -c "fe3a46" static/images/service-area-map.svg` returns at least 1 (Clark County)
- `grep -c "fe8a91" static/images/service-area-map.svg` returns at least 2 (Cowlitz + Skamania)
- `grep -c "323232" static/images/service-area-map.svg` returns at least 1 (pin marker)
  </verify>
  <done>
- SVG has Clark County in primary red (#fe3a46)
- Cowlitz and Skamania in lighter red (#fe8a91)
- Other counties in light gray (#e0e0e0)
- Battle Ground pin marker present
  </done>
</task>

<task type="auto">
  <name>Task 3: Export PNG from SVG</name>
  <files>static/images/service-area-map.png</files>
  <action>
Convert SVG to PNG for production use:

Option 1 - Using Inkscape CLI (if available):
```bash
inkscape static/images/service-area-map.svg --export-filename=static/images/service-area-map.png --export-width=800
```

Option 2 - Using ImageMagick (if available):
```bash
convert -background none -density 150 static/images/service-area-map.svg static/images/service-area-map.png
```

Option 3 - Using librsvg (if available):
```bash
rsvg-convert -w 800 static/images/service-area-map.svg > static/images/service-area-map.png
```

Target dimensions: 800px width (height proportional to maintain aspect ratio)

If no CLI tools available, the SVG can be kept as-is since browsers render SVG directly. In that case:
1. Update data/service-area.json to reference .svg instead of .png
2. Note this in the summary for user awareness
  </action>
  <verify>
- `ls -la static/images/service-area-map.png` shows file > 10KB (or .svg if PNG conversion unavailable)
- `file static/images/service-area-map.png` shows PNG image data (or SVG)
  </verify>
  <done>
- PNG file created from SVG at appropriate resolution
- File is valid image format
- OR: SVG used directly with data file updated accordingly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Service area map image showing Washington state with Clark County in red, Cowlitz/Skamania in lighter red, and a pin marker on Battle Ground</what-built>
  <how-to-verify>
1. Start Hugo dev server: `hugo server`
2. Open http://localhost:1313/#service-area
3. Verify the map image shows:
   - Full Washington state outline visible
   - Clark County clearly highlighted in red (#fe3a46)
   - Cowlitz and Skamania counties visible in lighter red
   - Battle Ground pin/marker icon present
   - Clean, modern flat illustration style
4. Check mobile view (resize browser to ~375px width) - map should scale appropriately
  </how-to-verify>
  <resume-signal>Type "approved" if map looks good, or describe what needs adjustment</resume-signal>
</task>

</tasks>

<verification>
```bash
# Verify files exist
ls -la static/images/service-area-map.*

# Check SVG has required colors
grep -E "(fe3a46|fe8a91|323232)" static/images/service-area-map.svg

# Visual verification via Hugo
hugo server
# Then check http://localhost:1313/#service-area
```
</verification>

<success_criteria>
- static/images/service-area-map.svg exists with styled counties
- static/images/service-area-map.png exists (or SVG used directly)
- Visual: Clark County prominent red, bordering counties lighter, pin on Battle Ground
- Map displays correctly in Hugo site at #service-area section
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-area-mapping/03-02-SUMMARY.md`
</output>
