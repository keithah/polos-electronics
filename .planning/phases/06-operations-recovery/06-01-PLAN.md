---
phase: 06-operations-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OPERATIONS.md]
autonomous: true

must_haves:
  truths:
    - "Operators can find documented steps to manually refresh reviews"
    - "Operators can verify site health using documented commands"
    - "Operators understand how fallback behavior protects users during failures"
  artifacts:
    - path: "OPERATIONS.md"
      provides: "Recovery runbook for operators"
      min_lines: 50
      contains: "Manual Review Refresh"
  key_links:
    - from: "OPERATIONS.md"
      to: ".github/workflows/fetch-reviews.yml"
      via: "workflow trigger documentation"
      pattern: "gh workflow run|Actions.*Fetch Reviews"
---

<objective>
Create OPERATIONS.md runbook documenting manual recovery procedures and health verification for the Polos Electronics static site.

Purpose: Enable operators to quickly diagnose and recover from review/map reliability incidents without code changes (satisfies OPER-02).
Output: OPERATIONS.md at repository root with recovery procedures and health verification commands.
</objective>

<execution_context>
@/Users/keith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-operations-recovery/06-CONTEXT.md
@.planning/phases/06-operations-recovery/06-RESEARCH.md
@.github/workflows/fetch-reviews.yml
@.github/workflows/hugo.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OPERATIONS.md runbook</name>
  <files>OPERATIONS.md</files>
  <action>
Create OPERATIONS.md at the repository root with the following sections:

1. **Quick Reference Table** - One-liner commands for common tasks:
   - Trigger review refresh: `gh workflow run fetch-reviews.yml`
   - Check workflow status: `gh run list --workflow=fetch-reviews.yml --limit 5`
   - Verify reviews on live site: `curl -s https://poloselectronics.com | grep -q "What Our Customers Say" && echo "OK"`
   - Verify map on live site: `curl -s https://poloselectronics.com | grep -q "service-area-map" && echo "OK"`

2. **Manual Review Refresh** - Step-by-step for triggering fetch-reviews.yml:
   - Via GitHub CLI (preferred): exact command
   - Via GitHub UI: numbered steps with exact clicks (Actions > Fetch Reviews > Run workflow)
   - Expected outcome: new commit with updated data/reviews.json

3. **Health Verification** - Commands to confirm site is healthy:
   - Check reviews section present
   - Check review cards rendered
   - Check service area map present
   - Check GitHub Actions workflow status

4. **Fallback Behavior** - Brief explanation of automatic fallback:
   - When live reviews array is empty, site shows fallbackReviews
   - No operator action needed - automatic
   - CI validates both reviews and fallbackReviews arrays exist

5. **Understanding CI Failures** - Reference (not duplication) to CI error messages:
   - Where to find: Actions tab, failed workflow run logs
   - What errors mean: validation step names describe the check
   - Point to hugo.yml for details (don't duplicate the logic)

Keep it concise: ~60-80 lines. Scannable headers, bullet points, code blocks.
Do NOT over-document. This is a simple static site.
  </action>
  <verify>
- `test -f OPERATIONS.md` returns success
- `wc -l OPERATIONS.md` returns between 50-100 lines
- `grep -q "Manual Review Refresh" OPERATIONS.md` returns success
- `grep -q "Health Verification" OPERATIONS.md` returns success
- `grep -q "gh workflow run" OPERATIONS.md` returns success
  </verify>
  <done>
OPERATIONS.md exists at repository root with:
- Quick reference table
- Manual review refresh procedures (CLI and UI)
- Health verification commands
- Fallback behavior explanation
- CI failure reference
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify documentation completeness</name>
  <files>OPERATIONS.md</files>
  <action>
Run verification checks to ensure OPERATIONS.md covers all required scenarios:

1. Confirm manual trigger documentation exists:
   - Search for `gh workflow run` command
   - Search for GitHub UI instructions

2. Confirm health check documentation exists:
   - Search for `curl` commands
   - Search for verification commands

3. Confirm fallback behavior is explained:
   - Search for `fallbackReviews` or "fallback" explanation

4. Test that documented commands work:
   - Run `gh workflow run fetch-reviews.yml --dry-run` or verify syntax
   - Run the curl health checks against live site

If any gaps found, update OPERATIONS.md to address them.
  </action>
  <verify>
- All documented curl commands execute successfully against live site
- OPERATIONS.md contains all 5 required sections
- `hugo server` still builds successfully (no regressions)
  </verify>
  <done>
OPERATIONS.md is complete and all documented commands work correctly.
  </done>
</task>

</tasks>

<verification>
- [ ] OPERATIONS.md exists at repository root
- [ ] Document contains Quick Reference, Manual Review Refresh, Health Verification, Fallback Behavior, CI Failures sections
- [ ] Documented commands are accurate and functional
- [ ] Document is concise (50-100 lines, not enterprise-length)
- [ ] Hugo build still works: `hugo --gc --minify`
</verification>

<success_criteria>
1. Operators can find OPERATIONS.md in repository root
2. Operators can trigger manual review refresh using documented steps
3. Operators can verify site health using documented commands
4. Operators understand automatic fallback behavior
5. No code changes - documentation only
</success_criteria>

<output>
After completion, create `.planning/phases/06-operations-recovery/06-01-SUMMARY.md`
</output>
